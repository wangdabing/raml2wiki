h3. {{wikisafe parentUrl}}{{wikisafe relativeUri}}
{{#methods}}
h4.  {{method}} {{#if description}} : {{description}}{{/if}}



{{#if headers}}
h5. Request Headers

|| Param Name || Type || Required || Example || Description ||
{{#each headers}}
| {{{@key}}} | {{{type}}} | {{{emote required}}} | {{{example}}} | {{{description}}} |
{{/each}}

{{/if}}


{{#if queryParameters}}
h5. Query Parameters

|| Param Name || Type || Required || Example || Default || Description ||
{{#each queryParameters}}
| {{{@key}}} | {{{type}}} | {{{emote required}}} | {{{example}}} | {{{default}}} | {{{description}}} |
{{/each}}

{{/if}}

{{#if body}}

h5. Body

{{#each body}}

h6. {{@key}}

{{#if formParameters}}
h6. Form Parameters

|| Param Name || Type || Required || Example || Default || Description ||
{{#each formParameters}}
| {{{@key}}} | {{{type}}} | {{{emote required}}} | {{{example}}} | {{{default}}} | {{{description}}} |
{{/each}}

{{/if}}
{{/each}}
{{/if}}

{{#if responses}}

h5. Responses

   {{#each responses}}
     h6. {{@key}} {{#if this.description}} - {{this.description}}{{/if}}

         {{#if this.headers}}
                                               
               {{#each this.headers}}
                    headers
               {{/each}}
                                             
         {{/if}}

         {{#if this.body}}
              {{#each this.body}}
                   Type: {{@key}}



              {{#if this.schema}}
             	{code:title=Schema {{@key}} |collapse=true }
{{{this.schema}}}
             	{code}
              
              {{/if}}

              {{#if this.example}}
              	{code:title=Example {{@key}} |collapse=true }
{{{this.example}}}
             	{code}
              
                 
              {{/if}}
             {{/each}}
        {{/if}}
     {{/each}}
                                    
    {{/if}}







{{/methods}}



{{#resources}}
{{> resource}}
{{/resources}}
